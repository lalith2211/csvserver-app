version: '3.8'

services:
  csvserver:
    image: infracloudio/csvserver:latest
    ports:
      - "9393:9300"
    volumes:
      - /csvserver/inputFile:/csvserver/inputdata
    environment:
      - CSVSERVER_BORDER=Orange
    restart: always
